///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

let problemOne = array => {
  for (let i = 0; i < array.length; i++) {
    array[i] = "whats my time complexity?";
  }
};

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

let problemTwo = array2 => {
  for (let i = 0; i < array2.length; i++) {
    console.log(i, "count");
  }
  for (let x = 0; x < array2.length; x++) {
    console.log("things are getting tricky!");
  }
};

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

let problemThree = array => {
  for (let i = 0; i < array.length; i++) {
    for (let x = 0; x < array.length; x++) {
      console.log(x, "x");
    }
  }
};
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

let problemFour = (sortedArray, n) => {
  let mid = Math.floor(sortedArray.length / 2);

  while (sortedArray[mid] !== n) {
  if (sortedArray[mid] < n) {
    sortedArray = sortedArray.slice(0, mid);
    mid = Math.floor(sortedArray.length / 2);
  } else if (sortedArray[mid] > n) {
    sortedArray = sortedArray.slice(mid);
    mid = Math.floor(sortedArray.length / 2);
  }
  return mid
};
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

let problemFour = array => {
  for (let x = 0; x < array.length; x++) {
    array.forEach(element => {
      return (array[x] = array[x] + element);
    });
  }
};

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

let problemFive = array => {
  let first = array[0];
  let last = array[array.length - 1];
  for (let x = 0; x < 100000; x++) {
    console.log(first, "yay!", last);
  }
  console.log(first, last, "yay!");
};
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

problemSix = array => {
  count = 0;
  for (x = 0; x < 100; x++) {
    if (array.includes(x)) {
      count++;
    }
  }
  return count;
};

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

problemSeven = n => {
  for (let x = 1; x < n; x++) {
    for (let y = 0; y < n; y += x) {
      console.log("look closer");
    }
  }
};

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

problemEight = (array, n) => {
  return array[n];
};

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


problemNine = (n) => {
  if(n<=1) {
    return n
  }
  return problemNine(n-2) + problemNine(n-1)
}
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
someArray [2, 4, "pickle", 9, ...args]

problemTen = (n) => {
  someArray.push(n)
}



///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

problemEleven = value => {
  putInHashTable(value)
}

